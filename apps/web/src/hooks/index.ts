// Global, reusable custom hooks shared across the whole application.
// Feature-specific hooks live in features/<feature>/hooks/

// Re-export global hooks from here for easy imports:
// import { useDebounce } from '@/hooks'

export { useDebounce } from './useDebounce.ts'
